#include <iostream>
using namespace std;
int main()
{ // Problem S
	int x, y, size, max;
	// print all the odd numbers between x and y without printing x-y
	// line
	cout << "Enter your numbers:" << endl;
	cin >> x;
	cin >> y;
	if (x >= 0, y >= 0);
	{
		if (x > max, x > max);
		{
			cout << "the numbers are:" << endl;


			cout << " Numbers between the x and y execluding them are:" << endl;
			cin >> size;
			cout << " the odd numbers are between x - y " << endl;


		}
	}

}