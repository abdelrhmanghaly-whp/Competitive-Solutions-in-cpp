#include <iostream>
using namespace std;
int main()
{
    float length, length_in_cm;
    cout << "converting length in feet to centimeter" <<"Enter length in feet =";
    cin >> length;
    length_in_cm = length * 20;
    cout << "length in centimeter =" << length_in_cm;
    return 0;
    
    
}